{
  "buildCommand": "rm -rf dist && mkdir -p dist/public && cp *.html *.css *.js dist/ && cp -r public/* dist/public/ && base64 -d public/css-logo-final.b64 > dist/public/css-logo.png",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "trailingSlash": false,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {"key": "X-Content-Type-Options", "value": "nosniff"},
        {"key": "Referrer-Policy", "value": "strict-origin-when-cross-origin"}
      ]
    }
  ]
}
